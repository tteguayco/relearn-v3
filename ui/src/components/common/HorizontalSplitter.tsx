import { ReflexSplitter } from 'react-reflex';

export const HorizontalSplitter = () => {
  return (
    <ReflexSplitter
      style={{
        borderTop: 0,
        borderBottom: 0,
      }}
    />
  );
};
