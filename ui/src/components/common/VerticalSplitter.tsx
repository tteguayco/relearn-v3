import { ReflexSplitter } from 'react-reflex';

export const VerticalSplitter = () => {
  return (
    <ReflexSplitter
      style={{
        borderLeft: 0,
        borderRight: 0,
      }}
    />
  );
};
